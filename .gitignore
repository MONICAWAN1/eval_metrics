# data & artifacts (never committed)
outputs/
logs/
ckpts/
docs/
artifacts/        # curated heavy eval outputs (generated cells, classifier ckpts), model-keyed
data/             # input slides + prepared intermediates
*.pkl
*.ckpt
*.h5ad

# `reports/` (saved metric outputs: .csv/.md) is intentionally tracked — a committed eval record.

# python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# notebooks scratch
notebooks/.ipynb_checkpoints/
src/paired_slides_eval/validation.py
.claude/
