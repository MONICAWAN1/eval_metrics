defaults:
  - ct_abca

datamodule:
  # KNN neighbourhood size: k = number of neighbours around the centroid (niche = k+1 points).
  # The classifier is expression-only; coordinates only select the KNN membership.
  n_neighbors: 10
