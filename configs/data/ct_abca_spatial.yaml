defaults:
  - ct_abca

# Spatial coordinate dim, consumed by the spatial classifier net.
coord_dim: 2

datamodule:
  # Microenvironment size (nearest neighbours per cell, self included). The ABCA
  # slides have ~41-43 neighbours per microenvironment, so 32 is safely below that.
  n_neighbors: 32
